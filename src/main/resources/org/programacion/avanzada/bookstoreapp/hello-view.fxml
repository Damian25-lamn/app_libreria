<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.shape.Circle?>
<?import javafx.scene.paint.Color?>

<VBox fx:id="rootVBox"
      alignment="CENTER"
      spacing="40"
      xmlns="http://javafx.com/javafx/8.0.171"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="org.programacion.avanzada.bookstoreapp.HelloController"
      style="-fx-background-color: #0b1e2d; -fx-padding: 60;">

    <!-- Logo con fondo circular -->
    <StackPane>
        <Circle radius="80" fill="#e0f4ff" />
        <ImageView fx:id="logoImageView"
                   fitWidth="120"
                   preserveRatio="true"
                   style="-fx-effect: dropshadow(three-pass-box, black, 8, 0, 0, 4);">
            <image>
                <Image url="@/images/logo-u.png" />
            </image>
        </ImageView>
    </StackPane>

    <!-- Título -->
    <Label text="📚 Bienvenido a la Librería"
           style="-fx-font-size: 28px; -fx-text-fill: white; -fx-font-weight: bold;" />

    <!-- Cuadro del grupo -->
    <VBox alignment="CENTER"
          spacing="8"
          style="-fx-background-color: #1f2f40; -fx-padding: 20; -fx-background-radius: 20; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.4), 10, 0, 0, 5);">
        <Label text="Grupo 6"
               style="-fx-text-fill: #ecf0f1; -fx-font-size: 20px; -fx-font-weight: bold;" />
        <Label text="- Minda Anthony"
               style="-fx-text-fill: #bdc3c7; -fx-font-size: 16px;" />
        <Label text="- Villarreal Santiago"
               style="-fx-text-fill: #bdc3c7; -fx-font-size: 16px;" />
        <Label text="- Maldonado Santiago"
               style="-fx-text-fill: #bdc3c7; -fx-font-size: 16px;" />
    </VBox>

    <!-- Botón -->
    <Button fx:id="acceder"
            text="Acceder"
            onAction="#onAccederClick"
            style="-fx-font-size: 18px; -fx-padding: 12 40; -fx-background-radius: 30; -fx-background-color: #3498db; -fx-text-fill: white;" />
</VBox>
